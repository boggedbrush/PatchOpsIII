# The repo root ignores `*.txt`, but the website needs `robots.txt` and `security.txt`.
!*.txt
!.well-known/
!.well-known/*.txt


# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
